<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>IKEA</title>
  <link rel="icon" href="/public/images/fav/favicon.ico" type="image/x-icon"> <!-- Favicon -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-dark" style="background-color: #004F9E;">
        <a class="navbar-brand d-flex align-items-center fw-bold fs-4 text-white" href="/">
          <img src="/public/images/ikea.png" alt="IKEA Logo" height="40" class="me-2">
        </a>

        <div class="d-flex align-items-center">
          <a href="/about" class="nav-link text-white me-3">About Us</a>
          <a href="/contact" class="nav-link text-white me-3">Contact Us</a>
      
          <% if (user) { %>
            <% if (user.role === 'admin') { %>
              <a href="/products" class="nav-link text-white me-3">Dashboard</a>
              <a href="/orders/admin/orders" class="nav-link text-white me-3">Orders</a>
            <% } else { %>
              <a href="/products" class="nav-link text-white me-3">Shop</a>
              <a href="/cart" class="nav-link text-white me-3">Cart</a>
              <a href="/orders/my" class="nav-link text-white me-3">My Orders</a>
            <% } %>
            <a href="/logout" class="btn btn-outline-light btn-sm">Logout</a>
          <% } else { %>
            <a href="/login" class="btn btn-outline-light btn-sm me-2">Login</a>
            <a href="/register" class="btn btn-outline-light btn-sm">Register</a>
          <% } %>
        </div>
      </nav>
      
      <% if (user) { %>
        <div class="container mt-2">
          <p class="text-end text-muted">Welcome, <%= user.name || user.email %></p>
        </div>
      <% } %>
</body>
</html>